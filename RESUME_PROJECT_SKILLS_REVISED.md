# 简历项目与专业技能精简稿

## 专业技能

- **Agent 工程：** 熟悉 Python、asyncio、Agent Loop、Tool Calling 与 MCP，具备权限感知 RAG、工具执行控制、确定性评测及安全边界设计经验。
- **Java 后端：** 熟悉 Java、Spring Boot、MyBatis-Plus，能够围绕并发控制、幂等处理与最终一致性完成业务系统设计。
- **存储与中间件：** 熟悉 MySQL 事务与索引、Redis 缓存/分布式锁/Lua 脚本，以及 RocketMQ 事务消息、顺序消费和失败重试。
- **英语能力：** CET-6 581，能够阅读英文技术文档并进行日常交流。

## 项目经历

### Aegis-Agent｜权限隔离型企业级 AI Agent

`2026.3 - 2026.6`

基于 Nanobot 面向企业群聊构建多用户 Agent，围绕身份隔离、权限检索、可靠执行与可复现评测完成安全硬化。

**技术栈：** Python / asyncio / SQLite / hnswlib / BGE Reranker / MCP

- **可信身份与隔离：** 身份仅由渠道签名链路注入，基于 `tenant/principal/audience` 的 SQL 授权谓词与 DB CHECK 在召回前完成可见性过滤；扩展至 8 类跨主体攻击后，Memory/History 暴露由 40 降至 0。
- **并发与上下文治理：** 基于异步运行时设计 `FairAdmissionController`，以会话锁前置准入、队列上限、单主体配额和过载拒绝避免请求无界堆积，压测中队列峰值由 117 降至 32；将长期记忆持久化保存、每轮仅注入可裁剪副本，结合多级压缩、Dream 记忆蒸馏与技能渐进式披露控制上下文占用。
- **权限感知 RAG：** 按 ACL 构建分区 HNSW 索引，先授权再执行 BM25 + 向量召回与 RRF 融合，并用 BGE Cross-Encoder 重排；固定 Embedding 下 nDCG@5 由 0.788 提升至 0.894，越权候选触达与事实暴露均为 0。
- **可靠工具执行：** 将 LLM 规划与确定性执行解耦，采用 `prepare/commit` 两阶段确认、`operation_id` 幂等、版本令牌和审计日志，覆盖飞书 7 类写操作，降低重复提交与上下文漂移风险。
- **确定性评测：** 构建 Agent、RAG、Security 三类评测 Board，以事实级 Gold、语义改写和同主题硬负例替代宽松关键词判分，证据落 JSON 可复算；Prompt Injection 反事实 A/B 中攻击成功数由 12/12 降至 0/12。

### 趣盒闪购｜盲盒潮玩高并发限时抢购平台

`2025.12 - 2026.2`

**技术栈：** Spring Boot / MySQL / Redis / Lua / RocketMQ / Caffeine / MyBatis-Plus / JMeter

- **概率分配与原子库存扣减：** 提出“预生成配额池”，备货期将 SKU 展开为乱序序列，把加权随机转化为无放回抽样，保证概率精确可审计；运行时经 Redis + Lua 原子 LPOP 完成款式分配与库存扣减，杜绝超卖。
- **热 Key 与稀有款公平性：** 针对秒杀热 Key 瓶颈设计配额池分片；针对“分片提升并发、但隐藏款仅 1 个无法分片”矛盾，设计跨分片稀有池，通过全局计数器 + 触发位置 Hash 将稀有款精准随机到固定，解释空间热点与概率公平。
- **异步架构与最终一致性：** 重构秒杀链路为“快速响应 + 异步处理”，通过 Redis + Lua 原子预扣、RocketMQ 事务消息与 MySQL 落库保证最终一致，消费端配合幂等校验、失败重试及延迟消息实现超时未支付订单自动关单与库存回滚；消除 MySQL 行锁竞争，JMeter 压测下单接口 RT 由 480 ms 降至 43 ms，吞吐量提升约 6 倍。
- **缓存架构设计：** 热点款式详情采用逻辑过期防击穿，不存在商品 ID 缓存空值（TTL 随机抖动 30~60s）解决穿透，无效请求拦截率 100%；Caffeine + Redis 二级缓存经 Pub/Sub 保证多实例一致性。
- **可靠性与业务闭环：** 配额池落库 DB 快照 + 按点库存差值恢复，支持 Redis 宕机重建；基于乐观锁处理揭晓幂等与超时关单，结合系列收集、揭晓广播、好友交换构建完整用户闭环。

## 压缩说明

- Aegis-Agent 原“确定性身份”“权限感知 RAG”“确定性评测”均在重复描述越权为 0，现分别承担安全控制面、检索质量和证据体系三个角色。
- 将公平准入和长任务恢复合并为“运行时治理”，避免拆成两个基础设施条目。
- 秒杀项目删除“业务闭环”条目；它能说明产品完整性，但对后端与 Agent 岗位的技术区分度最低。
- 秒杀项目将异步架构、库存一致性和性能指标并入同一条，避免 RocketMQ、Redis 预扣、MySQL 落库反复出现。
- 专业技能允许与项目技术栈重复，但只保留能概括跨项目能力的内容；项目技术栈负责关键词检索，技能栏负责说明掌握层次。
